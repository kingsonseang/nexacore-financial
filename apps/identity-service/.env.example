DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nexacore_identity
GRPC_PORT=50051
# JWT secrets stay empty locally until you generate them — openssl rand -base64 64 works for both
JWT_SECRET=
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_EXPIRES_IN=7d
